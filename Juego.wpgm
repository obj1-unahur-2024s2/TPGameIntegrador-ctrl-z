import wollok.game.*
import Jugador.*
import NPC.*
import Items.*
program pruebas{
    game.width(10)
    game.height(10)
    game.cellSize(50)
    game.addVisualCharacter(katos)
    game.addVisual(llave)
    game.addVisual(enemigo1)
    game.addVisual(espada)
    katos.tocarItem(espada)
    katos.tocarItem(llave)
    game.onCollideDo(enemigo1, {katos => katos.pelea(enemigo1)})
    
    game.title("katos")
    game.start()
    
}

